info:
  name: signup developer
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/api/auth/signup"
  body:
    type: form-urlencoded
    data:
      - name: full_name
        value: Test Developer
      - name: email
        value: test@phantasmagoria.com
      - name: password
        value: password123
      - name: confirm_password
        value: password123
  auth:
    type: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
